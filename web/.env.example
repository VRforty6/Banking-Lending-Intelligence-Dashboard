# The app reads the repository-level .env during local development.
# In production, inject these values into the server runtime.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=banking_lending_intelligence
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_SSL=false
