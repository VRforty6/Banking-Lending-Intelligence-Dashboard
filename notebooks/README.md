# Notebooks Directory

This directory is for **exploratory data analysis (EDA) only**. 

**Important**: Production-grade data transformations, ETL pipelines, and model training code must **not** be placed in notebooks. Such code should reside in the `src/` directory as proper Python modules or scripts to ensure reproducibility, testability, and maintainability.

Notebooks are for exploration, visualization, and initial prototyping only.